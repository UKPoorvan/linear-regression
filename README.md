# linear-regression
This project implements a Linear Regression model using Python and scikit-learn to predict continuous values based on input features. The model includes data preprocessing, train-test splitting, performance evaluation using R² score, and model serialization for reuse and deployment.
